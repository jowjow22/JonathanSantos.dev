---
phase: 05.2-case-study-url-form-field
plan: "01"
subsystem: ui
tags: [react, react-hook-form, zod, shadcn-ui, admin, forms]

# Dependency graph
requires:
  - phase: 04-content-sections
    provides: case_study_url DB column, database.types.ts Row/Insert/Update types, and public detail page rendering already in place
  - phase: 05.1-env-admin-quick-fixes
    provides: Admin sidebar and project form baseline used here
provides:
  - ProjectForm with case_study_url Zod schema entry, defaultValues entries (edit + create), form.reset() sync, and FormField JSX
affects: [05.2-case-study-url-form-field, admin project management, content creation workflow]

# Tech tracking
tech-stack:
  added: []
  patterns: [refine-or-empty URL validation pattern (v === '' || z.url().safeParse(v).success) — allows optional URL fields to accept blank submission without validation error]

key-files:
  created: []
  modified:
    - src/components/admin/ProjectForm/ProjectForm.tsx

key-decisions:
  - "case_study_url uses the same refine-or-empty URL validator pattern as live_url/repo_url — z.string().url().optional() was explicitly avoided because it rejects empty string submitted by unfilled inputs"

patterns-established:
  - "Refine-or-empty URL pattern: .refine((v) => v === '' || z.url().safeParse(v).success, { message: 'Invalid URL' }).optional() — standard for all optional URL fields in ProjectForm"

requirements-completed: [ADMN-03, CONT-02]

# Metrics
duration: 1min
completed: 2026-02-28
---

# Phase 05.2 Plan 01: Case Study URL Form Field Summary

**Case Study URL admin form field added to ProjectForm — 5 surgical additions (schema, 2x defaultValues, reset, JSX) close gap F-01 without touching service layer or DB**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-02-28T21:19:43Z
- **Completed:** 2026-02-28T21:20:28Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Added `case_study_url` Zod field with refine-or-empty URL validator matching the `live_url`/`repo_url` pattern — blank submission accepted, invalid URLs rejected
- Added `case_study_url` to `defaultValues` in both the edit branch (from `project.case_study_url ?? ''`) and create branch (empty string) so form initializes correctly in both modes
- Added `case_study_url` to `form.reset()` in the `useEffect` that syncs when server data refreshes — ensures re-opening the edit form shows the persisted DB value
- Added "Case Study URL" `FormField` JSX between the Repository URL and Display Order fields — positioned exactly where the plan specified

## Task Commits

Each task was committed atomically:

1. **Task 1: Add case_study_url to ProjectForm schema, defaultValues, reset, and JSX** - `12a18f4` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `src/components/admin/ProjectForm/ProjectForm.tsx` - Added 5 items: Zod schema entry, edit defaultValues entry, create defaultValues entry, form.reset() sync entry, and FormField JSX block for case_study_url

## Decisions Made

- Used the identical `.refine((v) => v === '' || z.url().safeParse(v).success)` pattern as `live_url` and `repo_url` — NOT `z.string().url().optional()` which would reject empty string submitted by an unfilled HTML input

## Deviations from Plan

None - plan executed exactly as written. All 5 additions made, no existing lines changed.

## Issues Encountered

None. TypeScript check (`npx tsc --noEmit`) shows no new errors — only the pre-existing TS2322 resolver incompatibility documented in STATE.md, which predates this phase.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- F-01 gap is now closed — admin can manage `case_study_url` via the admin form
- No DB migration needed (column already existed from Phase 4)
- No service layer changes needed (updateProject/createProject already pass form data directly to mutations)
- Public detail page already renders the Case Study link when the value is set — end-to-end flow complete
- Ready for Phase 6 (SEO & Analytics)

---
*Phase: 05.2-case-study-url-form-field*
*Completed: 2026-02-28*

## Self-Check: PASSED

- FOUND: src/components/admin/ProjectForm/ProjectForm.tsx
- FOUND: .planning/phases/05.2-case-study-url-form-field/05.2-01-SUMMARY.md
- FOUND: commit 12a18f4
